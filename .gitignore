# Runtime data (SQLite DB, geo database) — never commit
data/
*.db
*.db-wal
*.db-shm
*.mmdb

# Server secrets (.env holds DOMAIN, keys on the VPS)
.env

# Python
__pycache__/
*.pyc
.venv/

# OS / editor
.DS_Store

# sandbox-persistent venv (platform snapshots exclude .venv but not venv)
venv/
